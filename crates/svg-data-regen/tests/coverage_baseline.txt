# Coverage-gap baseline: attributes whose value space is `unresolved` — a value
# space we expect to model but have not derived from a readable source (distinct
# from `free_text`: event handlers, `data-*`, ARIA strings, polymorphic SMIL
# values). The gate asserts live `unresolved` == this file.
#
# This baseline is empty: every attribute value space now resolves from a read
# source (SVG 2 / SVG 1.1 / CSS / WAI-ARIA / WHATWG HTML). A new `unresolved`
# attribute — an upstream shape we stopped parsing — fails the gate loudly until
# it is either derived or recorded here with the source it awaits.
