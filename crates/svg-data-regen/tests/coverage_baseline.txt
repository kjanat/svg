# Coverage-gap baseline: attributes whose value space is `unresolved` — a value
# space we expect to model but have not derived from a readable source (distinct
# from `free_text`: event handlers, `data-*`, ARIA strings, polymorphic SMIL
# values). The gate asserts live `unresolved` == this file.
#
# A new `unresolved` attribute — an upstream shape we stopped parsing — fails
# the gate loudly until it is either derived or recorded here with the source it
# awaits.
#
# `begin`/`end` cite the SMIL `begin-value-list`/`end-value-list` productions: a
# semicolon-separated list of offset/syncbase/event/repeat/accessKey/wallclock
# begin-values. We do not model that timing grammar yet, so they stay auditable
# here rather than collapsing to a bogus single-keyword enum of the production
# name.
begin
end
