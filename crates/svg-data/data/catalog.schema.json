{
  "$id": "https://github.com/kjanat/svg/raw/HEAD/crates/svg-data/data/catalog.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$defs": {
    "Baseline": {
      "description": "Baseline facts. A missing recognized status is never Limited availability.",
      "type": "object",
      "properties": {
        "high_date": {
          "description": "Optional milestone when the feature became Widely Available.",
          "anyOf": [
            {
              "$ref": "#/$defs/BaselineDate"
            },
            {
              "type": "null"
            }
          ]
        },
        "low_date": {
          "description": "Optional milestone when the feature became Newly Available.",
          "anyOf": [
            {
              "$ref": "#/$defs/BaselineDate"
            },
            {
              "type": "null"
            }
          ]
        },
        "raw_status": {
          "description": "Original baseline field encoded as JSON, preserving null and invalid types.",
          "type": [
            "null",
            "string"
          ]
        },
        "status": {
          "description": "Recognized tier, independently of whether either date can be parsed.",
          "anyOf": [
            {
              "$ref": "#/$defs/BaselineTier"
            },
            {
              "type": "null"
            }
          ]
        },
        "status_diagnostic": {
          "description": "Parsing diagnostic, separate from the upstream status.",
          "anyOf": [
            {
              "$ref": "#/$defs/BaselineDiagnostic"
            },
            {
              "type": "null"
            }
          ]
        },
        "support": {
          "description": "Web Features browser versions for this selected status, independent of BCD support.",
          "type": [
            "null",
            "object"
          ],
          "additionalProperties": {
            "type": "string"
          }
        }
      }
    },
    "BaselineDate": {
      "description": "A full upstream date, with a parsed value only when its meaning is recognized.",
      "type": "object",
      "required": [
        "raw"
      ],
      "properties": {
        "date": {
          "description": "Valid calendar date in YYYY-MM-DD form.",
          "type": [
            "null",
            "string"
          ]
        },
        "qualifier": {
          "description": "Recognized comparison qualifier; unknown prefixes are never approximated.",
          "anyOf": [
            {
              "$ref": "#/$defs/BaselineQualifier"
            },
            {
              "type": "null"
            }
          ]
        },
        "raw": {
          "description": "Original upstream string, including any qualifier or malformed input.",
          "type": "string"
        }
      }
    },
    "BaselineDiagnostic": {
      "description": "Why an upstream status did not yield a recognized tier.",
      "oneOf": [
        {
          "description": "The status block has dates but no baseline field.",
          "type": "string",
          "const": "missing"
        },
        {
          "description": "The baseline field is present but not false, low, or high.",
          "type": "string",
          "const": "unrecognized"
        }
      ]
    },
    "BaselineQualifier": {
      "description": "Known inexactness qualifier on a date or browser version.",
      "oneOf": [
        {
          "description": "On or before the given date/version.",
          "type": "string",
          "const": "before"
        },
        {
          "description": "On or after the given date/version.",
          "type": "string",
          "const": "after"
        },
        {
          "description": "Approximately the given date/version.",
          "type": "string",
          "const": "approximately"
        }
      ]
    },
    "BaselineTier": {
      "description": "A recognized upstream Baseline tier, independent of dates and BCD flags.\n\nImported from Web Features, never calculated from the selected desktop\nbrowser rows. Missing/unrecognized status is `None`, not [`Self::Limited`].\nThis interoperability signal is separate from SVG validity and svg's usage\nrecommendations. See the [user guide](https://github.com/kjanat/svg/blob/master/docs/baseline.md)\nand [upstream definition](https://github.com/web-platform-dx/web-features/blob/main/docs/baseline.md).",
      "oneOf": [
        {
          "description": "Widely Available (`\"high\"` upstream): meets the longer-term upstream criteria.",
          "type": "string",
          "const": "widely"
        },
        {
          "description": "Newly Available (`\"low\"` upstream): reaches the upstream core browser set.",
          "type": "string",
          "const": "newly"
        },
        {
          "description": "Non-Baseline (`false` upstream), including discouraged features.",
          "type": "string",
          "const": "limited"
        }
      ]
    },
    "BrowserFlag": {
      "description": "A complete BCD flag declaration.",
      "type": "object",
      "required": [
        "name",
        "type"
      ],
      "properties": {
        "name": {
          "description": "Flag or preference name.",
          "type": "string"
        },
        "type": {
          "$ref": "#/$defs/FlagKind",
          "description": "Required upstream category."
        },
        "value_to_set": {
          "description": "Required setting value, if supplied.",
          "type": [
            "null",
            "string"
          ]
        }
      }
    },
    "BrowserVersion": {
      "description": "One complete support statement. Collections normalize upstream singleton/array forms.",
      "type": "object",
      "properties": {
        "alternative_name": {
          "description": "Name used by this implementation.",
          "type": [
            "null",
            "string"
          ]
        },
        "flags": {
          "description": "All flag declarations.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/BrowserFlag"
          }
        },
        "impl_url": {
          "description": "Implementation tracking links.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "notes": {
          "description": "Original upstream notes, with HTML retained as data.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "partial_implementation": {
          "description": "Whether mandatory behavior is missing.",
          "default": false,
          "type": "boolean"
        },
        "prefix": {
          "description": "Required prefix.",
          "type": [
            "null",
            "string"
          ]
        },
        "version_added": {
          "description": "Upstream version union. Absent/null/invalid input remains unknown.",
          "anyOf": [
            {
              "$ref": "#/$defs/VersionAdded"
            },
            {
              "type": "null"
            }
          ]
        },
        "version_last": {
          "description": "Last version with support.",
          "type": [
            "null",
            "string"
          ]
        },
        "version_removed": {
          "description": "First version without support, retaining its qualifier.",
          "type": [
            "null",
            "string"
          ]
        }
      }
    },
    "CatalogAnimation": {
      "description": "How an attribute or property can be animated.\n\nSVG 2 propdefs cite the Web Animations `animation-type` (`by computed value`\n/ `discrete` / `not animatable`); SVG Animations derives the additive\nsemantics (`yes` / `yes (non-additive)` / `no`) from it. This tri-state\nreplaces a lossy `animatable: bool` that could not express the non-additive\n(`discrete`) case — animating such a value swaps discretely rather than\ninterpolating, and `<animate additive=\"sum\">` is invalid on it.",
      "oneOf": [
        {
          "description": "Cannot be animated (Web Animations `not animatable`).",
          "type": "string",
          "const": "not_animatable"
        },
        {
          "description": "Animatable but non-additive — only discrete swaps between values (Web\nAnimations `discrete`; SVG Animations `yes (non-additive)`).",
          "type": "string",
          "const": "discrete"
        },
        {
          "description": "Animatable and additive/interpolable (Web Animations `by computed value`;\nSVG Animations `yes`).",
          "type": "string",
          "const": "additive"
        }
      ]
    },
    "CatalogAttribute": {
      "description": "One attribute's spec-derived catalog entry.",
      "type": "object",
      "required": [
        "animation",
        "applicability",
        "name",
        "values"
      ],
      "properties": {
        "animation": {
          "$ref": "#/$defs/CatalogAnimation",
          "description": "How the attribute animates (Web Animations `animation-type` the SVG 2\npropdefs cite; SVG Animations additive semantics)."
        },
        "applicability": {
          "$ref": "#/$defs/CatalogAttributeApplicability",
          "description": "Which elements accept the attribute."
        },
        "baseline": {
          "description": "Web-platform baseline status, when known.",
          "anyOf": [
            {
              "$ref": "#/$defs/Baseline"
            },
            {
              "type": "null"
            }
          ]
        },
        "browser_support": {
          "description": "Per-browser support data, when known.",
          "type": [
            "null",
            "object"
          ],
          "additionalProperties": {
            "type": "array",
            "items": {
              "$ref": "#/$defs/BrowserVersion"
            }
          }
        },
        "deprecated": {
          "description": "Whether compat data marks the attribute deprecated.",
          "type": "boolean"
        },
        "description": {
          "description": "Short human-readable description from spec prose.",
          "type": [
            "null",
            "string"
          ]
        },
        "discouraged": {
          "description": "`WebDX` discouragement, retaining feature identity and compatibility context.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Discouraged"
          }
        },
        "element_compat": {
          "description": "Element-scoped compat facts for attributes whose BCD data differs by bearer.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogAttributeElementCompat"
          }
        },
        "element_values": {
          "description": "Element-scoped value-space overrides for attributes whose value grammar\ngenuinely differs by bearer element within the same profile (e.g.\n`operator` on `feComposite` vs `feMorphology`).",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogAttributeElementValues"
          }
        },
        "experimental": {
          "description": "Whether compat data marks the attribute experimental.",
          "type": "boolean"
        },
        "mdn_url": {
          "description": "MDN reference URL.",
          "type": [
            "null",
            "string"
          ]
        },
        "name": {
          "description": "Canonical attribute name (`xlink:href` collapses to `href`).",
          "type": "string"
        },
        "presentation_attribute": {
          "description": "Backing CSS property name when this is a presentation attribute.",
          "type": [
            "null",
            "string"
          ]
        },
        "spec_url": {
          "description": "Resolved spec permalink.",
          "type": [
            "null",
            "string"
          ]
        },
        "standard_track": {
          "description": "Whether compat data marks the attribute as standards-track.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "values": {
          "$ref": "#/$defs/CatalogAttributeValues",
          "description": "Attribute value space."
        }
      }
    },
    "CatalogAttributeApplicability": {
      "description": "Which elements an attribute can appear on.",
      "oneOf": [
        {
          "description": "Applies to every element that accepts global SVG attributes.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "global"
            }
          }
        },
        {
          "description": "Applies only to the listed element names.",
          "type": "object",
          "required": [
            "elements",
            "kind"
          ],
          "properties": {
            "elements": {
              "description": "Bearer element names.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "kind": {
              "type": "string",
              "const": "elements"
            }
          }
        },
        {
          "description": "Known attribute that applies to no elements in this catalog.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "none"
            }
          }
        }
      ]
    },
    "CatalogAttributeElementCompat": {
      "description": "Attribute compat facts scoped to one element bearer.",
      "type": "object",
      "required": [
        "element"
      ],
      "properties": {
        "baseline": {
          "description": "Web-platform baseline status, when known.",
          "anyOf": [
            {
              "$ref": "#/$defs/Baseline"
            },
            {
              "type": "null"
            }
          ]
        },
        "browser_support": {
          "description": "Per-browser support data, when known.",
          "type": [
            "null",
            "object"
          ],
          "additionalProperties": {
            "type": "array",
            "items": {
              "$ref": "#/$defs/BrowserVersion"
            }
          }
        },
        "deprecated": {
          "description": "Whether compat data marks the feature deprecated.",
          "type": "boolean"
        },
        "discouraged": {
          "description": "`WebDX` discouragement, retaining feature identity and compatibility context.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Discouraged"
          }
        },
        "element": {
          "description": "Element name this compat record applies to.",
          "type": "string"
        },
        "experimental": {
          "description": "Whether compat data marks the feature experimental.",
          "type": "boolean"
        },
        "mdn_url": {
          "description": "MDN reference URL.",
          "type": [
            "null",
            "string"
          ]
        },
        "standard_track": {
          "description": "Whether compat data marks the feature as standards-track.",
          "type": [
            "boolean",
            "null"
          ]
        }
      }
    },
    "CatalogAttributeElementValues": {
      "description": "One element-scoped value-space override for an attribute whose value grammar\ngenuinely differs by bearer element within the same profile.\n\nThe canonical [`CatalogAttribute::values`] holds the value space of the\nfirst bearer (by sorted element name); each diverging bearer is recorded\nhere so no spec-defined value space is lost.",
      "type": "object",
      "required": [
        "element",
        "values"
      ],
      "properties": {
        "element": {
          "description": "Element this value-space override applies to.",
          "type": "string"
        },
        "values": {
          "$ref": "#/$defs/CatalogAttributeValues",
          "description": "Value space when the attribute is borne by `element`."
        }
      }
    },
    "CatalogAttributeValues": {
      "description": "The runtime value-space shapes the catalog emits.",
      "oneOf": [
        {
          "description": "One of the listed keyword values.",
          "type": "object",
          "required": [
            "kind",
            "values"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "enum"
            },
            "values": {
              "description": "Allowed keyword values.",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        },
        {
          "description": "A transform list; functions are completion hints.",
          "type": "object",
          "required": [
            "functions",
            "kind"
          ],
          "properties": {
            "functions": {
              "description": "Transform function names.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "kind": {
              "type": "string",
              "const": "transform"
            }
          }
        },
        {
          "description": "A CSS/SVG color value.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "color"
            }
          }
        },
        {
          "description": "An SVG paint value: `none | <color> | <url> [none | <color>]? |\ncontext-fill | context-stroke`. Strictly richer than [`Color`]; used by\n`fill` and `stroke` so their non-color alternatives are not dropped.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "paint"
            }
          }
        },
        {
          "description": "A length or length-percentage value.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "length"
            }
          }
        },
        {
          "description": "A URL / fragment reference (ASCII, per the URL/URI grammar).",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "url"
            }
          }
        },
        {
          "description": "An SVG Internationalized Resource Identifier reference. A strict superset\nof [`Self::Url`] — IRIs permit non-ASCII (Unicode) characters that a URL\ndisallows, so the two are kept distinct rather than conflated.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "iri"
            }
          }
        },
        {
          "description": "A boolean attribute as defined by HTML.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "boolean"
            }
          }
        },
        {
          "description": "A space-separated token list.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "token_list"
            }
          }
        },
        {
          "description": "A comma-separated token list.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "comma_token_list"
            }
          }
        },
        {
          "description": "A space-separated list of URL tokens.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "url_token_list"
            }
          }
        },
        {
          "description": "A BCP 47 / ABNF language tag.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "language_tag"
            }
          }
        },
        {
          "description": "An integer value.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "integer"
            }
          }
        },
        {
          "description": "A MIME media type.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "media_type"
            }
          }
        },
        {
          "description": "A CSS media query list.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "media_query_list"
            }
          }
        },
        {
          "description": "A CSS declaration list, as used by inline `style`.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "css_declaration_list"
            }
          }
        },
        {
          "description": "An SVG element ID value.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "id"
            }
          }
        },
        {
          "description": "A referrer policy string.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "referrer_policy"
            }
          }
        },
        {
          "description": "A suggested download file name.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "suggested_file_name"
            }
          }
        },
        {
          "description": "SVG path data.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "path_data"
            }
          }
        },
        {
          "description": "A semicolon-separated list of numbers.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "semicolon_number_list"
            }
          }
        },
        {
          "description": "One motion-animation coordinate pair.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "coordinate_pair"
            }
          }
        },
        {
          "description": "A semicolon-separated list of motion-animation coordinate pairs.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "coordinate_pair_list"
            }
          }
        },
        {
          "description": "A number or percentage.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "number_or_percentage"
            }
          }
        },
        {
          "description": "A bare number value.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "number"
            }
          }
        },
        {
          "description": "A space-separated list of element ID references.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "id_list"
            }
          }
        },
        {
          "description": "A structured CSS value grammar that is richer than the runtime's\nspecialized variants.",
          "type": "object",
          "required": [
            "grammar",
            "graph",
            "kind"
          ],
          "properties": {
            "grammar": {
              "description": "Raw grammar text from the defining spec.",
              "type": "string"
            },
            "graph": {
              "$ref": "#/$defs/CatalogCssGrammarGraph",
              "description": "Token graph extracted from the grammar."
            },
            "kind": {
              "type": "string",
              "const": "css_grammar"
            }
          }
        },
        {
          "description": "Free-form text with no constrained grammar (genuinely unconstrained:\nevent handlers, `data-*`, ARIA string values).",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "free_text"
            }
          }
        },
        {
          "description": "A value space we expect to model but have not derived yet. Distinct from\n[`CatalogAttributeValues::FreeText`] so unparsed attributes are auditable\ninstead of hidden among genuinely free-form ones.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "unresolved"
            }
          }
        }
      ]
    },
    "CatalogCompatDocument": {
      "title": "CatalogCompatDocument",
      "description": "Browser-compat provenance and retained non-catalogued compat features.",
      "type": "object",
      "required": [
        "browser_compat_data",
        "schema_version",
        "web_features"
      ],
      "properties": {
        "browser_compat_data": {
          "$ref": "#/$defs/CatalogPackageSource",
          "description": "MDN browser-compat-data package source."
        },
        "schema_version": {
          "description": "Version of the JSON catalog/schema contract.",
          "type": "integer",
          "minimum": 0,
          "maximum": 65535,
          "format": "uint16"
        },
        "unmodeled_features": {
          "description": "BCD features intentionally not modeled as elements/attributes.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogCompatSubfeature"
          }
        },
        "web_features": {
          "$ref": "#/$defs/CatalogPackageSource",
          "description": "web-features package source."
        }
      }
    },
    "CatalogCompatSubfeature": {
      "description": "A BCD feature below an SVG element that is not an element or attribute.",
      "type": "object",
      "required": [
        "compat_key",
        "element",
        "kind",
        "name"
      ],
      "properties": {
        "baseline": {
          "description": "Web-platform baseline status, when known.",
          "anyOf": [
            {
              "$ref": "#/$defs/Baseline"
            },
            {
              "type": "null"
            }
          ]
        },
        "browser_support": {
          "description": "Per-browser support data, when known.",
          "type": [
            "null",
            "object"
          ],
          "additionalProperties": {
            "type": "array",
            "items": {
              "$ref": "#/$defs/BrowserVersion"
            }
          }
        },
        "compat_key": {
          "description": "Full BCD compat key, e.g. `svg.elements.use.data_uri`.",
          "type": "string"
        },
        "deprecated": {
          "description": "Whether compat data marks the feature deprecated.",
          "type": "boolean"
        },
        "discouraged": {
          "description": "`WebDX` discouragement, retaining feature identity and compatibility context.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Discouraged"
          }
        },
        "element": {
          "description": "Owning SVG element name.",
          "type": "string"
        },
        "experimental": {
          "description": "Whether compat data marks the feature experimental.",
          "type": "boolean"
        },
        "kind": {
          "$ref": "#/$defs/CatalogCompatSubfeatureKind",
          "description": "Why this was not modeled as a normal attribute/element."
        },
        "mdn_url": {
          "description": "MDN reference URL.",
          "type": [
            "null",
            "string"
          ]
        },
        "name": {
          "description": "BCD child feature name.",
          "type": "string"
        },
        "standard_track": {
          "description": "Whether compat data marks the feature as standards-track.",
          "type": [
            "boolean",
            "null"
          ]
        }
      }
    },
    "CatalogCompatSubfeatureKind": {
      "description": "Why a BCD child feature is kept out of the attribute catalog.",
      "oneOf": [
        {
          "description": "A behavior or value-shape feature, not an attribute name.",
          "type": "string",
          "const": "behavior"
        },
        {
          "description": "A legacy `xlink:*` alias that needs profile-scoped alias modeling.",
          "type": "string",
          "const": "legacy_xlink_alias"
        }
      ]
    },
    "CatalogContentModel": {
      "description": "The runtime content-model shapes the catalog emits. The spec's category\ntaxonomy is already flattened into [`Self::ChildrenSet`].",
      "oneOf": [
        {
          "description": "Accepts exactly the listed child element names.",
          "type": "object",
          "required": [
            "elements",
            "kind"
          ],
          "properties": {
            "elements": {
              "description": "Allowed child element names (sorted, deduped).",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "kind": {
              "type": "string",
              "const": "children_set"
            }
          }
        },
        {
          "description": "Accepts any element from the SVG namespace.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "any_svg"
            }
          }
        },
        {
          "description": "Hosts arbitrary foreign-namespace content (the spec's `any` model:\n\"any elements or character data\"), e.g. HTML in `foreignObject`.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "foreign"
            }
          }
        },
        {
          "description": "Primarily character data.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "const": "text"
            }
          }
        }
      ]
    },
    "CatalogCore": {
      "title": "CatalogCore",
      "description": "Canonical latest catalog data after semantic projection.\n\nThis document is the runtime-facing catalog, not a raw upstream XML/HTML mirror.",
      "type": "object",
      "required": [
        "attributes",
        "elements",
        "schema_version"
      ],
      "properties": {
        "attributes": {
          "description": "Attribute definitions, sorted by canonical name.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogAttribute"
          }
        },
        "elements": {
          "description": "Element definitions, sorted by name.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogElement"
          }
        },
        "schema_version": {
          "description": "Version of the JSON catalog/schema contract.",
          "type": "integer",
          "minimum": 0,
          "maximum": 65535,
          "format": "uint16"
        }
      }
    },
    "CatalogCssGrammarEdge": {
      "description": "One directed edge in a CSS grammar graph.",
      "type": "object",
      "required": [
        "from",
        "kind",
        "to"
      ],
      "properties": {
        "from": {
          "description": "Source node id.",
          "type": "integer",
          "minimum": 0,
          "maximum": 65535,
          "format": "uint16"
        },
        "kind": {
          "$ref": "#/$defs/CatalogCssGrammarEdgeKind",
          "description": "Edge kind."
        },
        "to": {
          "description": "Target node id.",
          "type": "integer",
          "minimum": 0,
          "maximum": 65535,
          "format": "uint16"
        }
      }
    },
    "CatalogCssGrammarEdgeKind": {
      "description": "CSS grammar edge kinds.",
      "oneOf": [
        {
          "description": "Parent/group containment.",
          "type": "string",
          "const": "contains"
        },
        {
          "description": "Sibling order within a parent/group.",
          "type": "string",
          "const": "next"
        }
      ]
    },
    "CatalogCssGrammarGraph": {
      "description": "Graph representation of a CSS value grammar.",
      "type": "object",
      "required": [
        "edges",
        "nodes",
        "root"
      ],
      "properties": {
        "edges": {
          "description": "Grammar edges.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogCssGrammarEdge"
          }
        },
        "nodes": {
          "description": "Grammar nodes.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogCssGrammarNode"
          }
        },
        "root": {
          "description": "Root node id.",
          "type": "integer",
          "minimum": 0,
          "maximum": 65535,
          "format": "uint16"
        }
      }
    },
    "CatalogCssGrammarNode": {
      "description": "One node in a CSS grammar graph.",
      "type": "object",
      "required": [
        "id",
        "kind"
      ],
      "properties": {
        "id": {
          "description": "Stable node id within the graph.",
          "type": "integer",
          "minimum": 0,
          "maximum": 65535,
          "format": "uint16"
        },
        "kind": {
          "$ref": "#/$defs/CatalogCssGrammarNodeKind",
          "description": "Node kind."
        },
        "text": {
          "description": "Token text, when the node carries one.",
          "type": [
            "null",
            "string"
          ]
        }
      }
    },
    "CatalogCssGrammarNodeKind": {
      "description": "CSS grammar node kinds.",
      "oneOf": [
        {
          "description": "Synthetic root.",
          "type": "string",
          "const": "root"
        },
        {
          "description": "Bracketed group.",
          "type": "string",
          "const": "group"
        },
        {
          "description": "CSS keyword token.",
          "type": "string",
          "const": "keyword"
        },
        {
          "description": "CSS type token, e.g. `<length>`.",
          "type": "string",
          "const": "type"
        },
        {
          "description": "Functional notation token, e.g. `url()`.",
          "type": "string",
          "const": "function"
        },
        {
          "description": "CSS grammar operator or multiplier.",
          "type": "string",
          "const": "operator"
        }
      ]
    },
    "CatalogDeclaredStatus": {
      "description": "Explicit specification lifecycle declarations.",
      "oneOf": [
        {
          "description": "Still defined, but deprecated for new content.",
          "type": "string",
          "const": "deprecated"
        },
        {
          "description": "Still defined for legacy content, but obsoleted.",
          "type": "string",
          "const": "obsolete"
        },
        {
          "description": "Explicitly removed from the specification.",
          "type": "string",
          "const": "removed"
        }
      ]
    },
    "CatalogElement": {
      "description": "One element's spec-derived catalog entry.",
      "type": "object",
      "required": [
        "attrs",
        "content_model",
        "global_attrs",
        "name"
      ],
      "properties": {
        "attrs": {
          "description": "Element-specific attribute names (sorted, deduped).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "baseline": {
          "description": "Web-platform baseline status, when known.",
          "anyOf": [
            {
              "$ref": "#/$defs/Baseline"
            },
            {
              "type": "null"
            }
          ]
        },
        "browser_support": {
          "description": "Per-browser support data, when known.",
          "type": [
            "null",
            "object"
          ],
          "additionalProperties": {
            "type": "array",
            "items": {
              "$ref": "#/$defs/BrowserVersion"
            }
          }
        },
        "content_model": {
          "$ref": "#/$defs/CatalogContentModel",
          "description": "Structural child-content model."
        },
        "deprecated": {
          "description": "Whether compat data marks the element deprecated.",
          "type": "boolean"
        },
        "description": {
          "description": "Short human-readable description from spec prose.",
          "type": [
            "null",
            "string"
          ]
        },
        "discouraged": {
          "description": "`WebDX` discouragement, retaining feature identity and compatibility context.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Discouraged"
          }
        },
        "experimental": {
          "description": "Whether compat data marks the element experimental.",
          "type": "boolean"
        },
        "global_attrs": {
          "description": "Whether the element carries the SVG global (`core`) attributes.",
          "type": "boolean"
        },
        "mdn_url": {
          "description": "MDN reference URL.",
          "type": [
            "null",
            "string"
          ]
        },
        "name": {
          "description": "Element tag name.",
          "type": "string"
        },
        "spec_url": {
          "description": "Resolved spec permalink (the module's anchor base joined with the href).",
          "type": [
            "null",
            "string"
          ]
        },
        "standard_track": {
          "description": "Whether compat data marks the element as standards-track.",
          "type": [
            "boolean",
            "null"
          ]
        }
      }
    },
    "CatalogFileRef": {
      "description": "Relative reference from one catalog document to another.",
      "type": "object",
      "required": [
        "href"
      ],
      "properties": {
        "href": {
          "description": "Relative path from `catalog.json` to the JSON file.",
          "type": "string"
        }
      }
    },
    "CatalogGraphDocument": {
      "title": "CatalogGraphDocument",
      "description": "Derived graph document.",
      "type": "object",
      "required": [
        "edges",
        "nodes",
        "schema_version"
      ],
      "properties": {
        "edges": {
          "description": "Directed graph edges, sorted by `(from, to, kind)`.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogGraphEdge"
          }
        },
        "nodes": {
          "description": "Graph nodes, sorted by id.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogGraphNode"
          }
        },
        "schema_version": {
          "description": "Version of the JSON catalog/schema contract.",
          "type": "integer",
          "minimum": 0,
          "maximum": 65535,
          "format": "uint16"
        }
      }
    },
    "CatalogGraphEdge": {
      "description": "One directed edge in the derived catalog graph.",
      "type": "object",
      "required": [
        "from",
        "kind",
        "to"
      ],
      "properties": {
        "from": {
          "description": "Source node id.",
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/CatalogGraphEdgeKind",
          "description": "Edge kind."
        },
        "to": {
          "description": "Target node id.",
          "type": "string"
        }
      }
    },
    "CatalogGraphEdgeKind": {
      "description": "Catalog graph edge kinds.",
      "oneOf": [
        {
          "description": "Parent element accepts child element.",
          "type": "string",
          "const": "allows_child"
        },
        {
          "description": "Element has a direct/specific attribute declaration.",
          "type": "string",
          "const": "has_attribute"
        },
        {
          "description": "Attribute applies to an element after global/scoped applicability is resolved.",
          "type": "string",
          "const": "applies_to"
        },
        {
          "description": "Feature is a member of a spec category.",
          "type": "string",
          "const": "member_of"
        },
        {
          "description": "Element accepts the SVG global attribute set.",
          "type": "string",
          "const": "accepts_global_attributes"
        },
        {
          "description": "Presentation attribute is backed by a CSS property.",
          "type": "string",
          "const": "uses_css_property"
        },
        {
          "description": "Attribute points at a value grammar node.",
          "type": "string",
          "const": "has_value_grammar"
        },
        {
          "description": "Value grammar override applies in the target profile.",
          "type": "string",
          "const": "overrides_value_in_profile"
        },
        {
          "description": "Compat subfeature describes the target element/attribute.",
          "type": "string",
          "const": "describes"
        },
        {
          "description": "Feature is present in the target profile.",
          "type": "string",
          "const": "present_in"
        }
      ]
    },
    "CatalogGraphNode": {
      "description": "One node in the derived catalog graph.",
      "type": "object",
      "required": [
        "id",
        "kind",
        "name"
      ],
      "properties": {
        "id": {
          "description": "Stable node id, namespaced by node kind.",
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/CatalogGraphNodeKind",
          "description": "Node kind."
        },
        "name": {
          "description": "Human-readable node name.",
          "type": "string"
        }
      }
    },
    "CatalogGraphNodeKind": {
      "description": "Catalog graph node kinds.",
      "oneOf": [
        {
          "description": "SVG element definition.",
          "type": "string",
          "const": "element"
        },
        {
          "description": "SVG attribute definition.",
          "type": "string",
          "const": "attribute"
        },
        {
          "description": "SVG element category from the spec taxonomy.",
          "type": "string",
          "const": "element_category"
        },
        {
          "description": "SVG attribute category from the spec taxonomy.",
          "type": "string",
          "const": "attribute_category"
        },
        {
          "description": "SVG specification profile/snapshot.",
          "type": "string",
          "const": "profile"
        },
        {
          "description": "CSS property backing a presentation attribute.",
          "type": "string",
          "const": "css_property"
        },
        {
          "description": "Attribute value grammar node.",
          "type": "string",
          "const": "value_grammar"
        },
        {
          "description": "Retained browser-compat subfeature not modeled as a first-class element/attribute.",
          "type": "string",
          "const": "compat_feature"
        }
      ]
    },
    "CatalogInventoryElement": {
      "description": "One element's per-snapshot attribute inventory.",
      "type": "object",
      "required": [
        "attributes",
        "name"
      ],
      "properties": {
        "attributes": {
          "description": "Attribute names this profile lists for the element.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "name": {
          "description": "Element name.",
          "type": "string"
        }
      }
    },
    "CatalogLifecycleDeclaration": {
      "description": "A normative lifecycle statement at an immutable specification location.",
      "type": "object",
      "required": [
        "source",
        "status"
      ],
      "properties": {
        "source": {
          "description": "Dated publication or commit-pinned source URL, including its anchor.",
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/CatalogDeclaredStatus",
          "description": "What the specification declares, independently of inventory membership."
        }
      }
    },
    "CatalogLifecycleEntry": {
      "description": "One feature lifecycle fact in a snapshot overlay.",
      "type": "object",
      "required": [
        "lifecycle",
        "name",
        "present"
      ],
      "properties": {
        "catalog_name": {
          "description": "Canonical catalog attribute name, when different from `name`.",
          "type": [
            "null",
            "string"
          ]
        },
        "declaration": {
          "description": "Explicit specification declaration, independent of browser advice.",
          "anyOf": [
            {
              "$ref": "#/$defs/CatalogLifecycleDeclaration"
            },
            {
              "type": "null"
            }
          ]
        },
        "known_in": {
          "description": "Snapshots where this feature is present.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogSpecSnapshotId"
          }
        },
        "lifecycle": {
          "$ref": "#/$defs/CatalogLifecycleStatus",
          "description": "Lifecycle status for this snapshot."
        },
        "name": {
          "description": "Feature name as written in this profile family (`xlink:href` stays\ndistinct from `href`).",
          "type": "string"
        },
        "owner": {
          "description": "Bearer element for an attribute-local declaration; absent means global.",
          "type": [
            "null",
            "string"
          ]
        },
        "present": {
          "description": "Whether the feature is present in the containing snapshot.",
          "type": "boolean"
        }
      }
    },
    "CatalogLifecycleStatus": {
      "description": "Lifecycle derived from profile membership and explicit spec declarations.",
      "oneOf": [
        {
          "description": "Present and stable, included only when the overlay must carry profile\nspelling metadata.",
          "type": "string",
          "const": "stable"
        },
        {
          "description": "Present only in a draft snapshot.",
          "type": "string",
          "const": "experimental"
        },
        {
          "description": "Present but explicitly deprecated by the specification.",
          "type": "string",
          "const": "deprecated"
        },
        {
          "description": "Explicitly obsoleted while retained, or removed from this edition.",
          "type": "string",
          "const": "obsolete"
        },
        {
          "description": "Known in later snapshots, absent from this one.",
          "type": "string",
          "const": "not_yet_introduced"
        }
      ]
    },
    "CatalogPackageSource": {
      "description": "One npm package source used during regeneration.",
      "type": "object",
      "required": [
        "name",
        "url",
        "version"
      ],
      "properties": {
        "name": {
          "description": "Package name.",
          "type": "string"
        },
        "url": {
          "description": "Exact data URL fetched.",
          "type": "string"
        },
        "version": {
          "description": "Resolved package version.",
          "type": "string"
        }
      }
    },
    "CatalogSnapshot": {
      "title": "CatalogSnapshot",
      "description": "Version-specific overlay document.",
      "type": "object",
      "required": [
        "inventory",
        "profile",
        "schema_version"
      ],
      "properties": {
        "aliases": {
          "description": "Accepted profile id aliases for this snapshot.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "inventory": {
          "$ref": "#/$defs/CatalogSnapshotInventory",
          "description": "Per-snapshot element/attribute inventory."
        },
        "lifecycle": {
          "$ref": "#/$defs/CatalogSnapshotLifecycle",
          "description": "Profile lifecycle facts derived from membership and explicit spec declarations."
        },
        "profile": {
          "$ref": "#/$defs/CatalogSpecSnapshotId",
          "description": "Profile snapshot this overlay describes."
        },
        "schema_version": {
          "description": "Version of the JSON catalog/schema contract.",
          "type": "integer",
          "minimum": 0,
          "maximum": 65535,
          "format": "uint16"
        },
        "sources": {
          "description": "Exact source URLs used to derive this snapshot.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "value_overrides": {
          "description": "Per-profile value-space overrides for this snapshot.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogSnapshotValueOverride"
          }
        }
      }
    },
    "CatalogSnapshotInventory": {
      "description": "Per-snapshot element/attribute inventory payload.",
      "type": "object",
      "required": [
        "attributes",
        "elements"
      ],
      "properties": {
        "attributes": {
          "description": "Attributes present anywhere in this profile.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "elements": {
          "description": "Elements present in this profile.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogInventoryElement"
          }
        }
      }
    },
    "CatalogSnapshotLifecycle": {
      "description": "Per-snapshot lifecycle overlay.",
      "type": "object",
      "properties": {
        "attributes": {
          "description": "Attribute lifecycle facts that differ from ordinary stable presence.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogLifecycleEntry"
          }
        },
        "elements": {
          "description": "Element lifecycle facts that differ from ordinary stable presence.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/CatalogLifecycleEntry"
          }
        }
      }
    },
    "CatalogSnapshotRef": {
      "description": "Reference from the root catalog to a version-specific overlay document.",
      "type": "object",
      "required": [
        "href",
        "profile"
      ],
      "properties": {
        "href": {
          "description": "Relative path from `catalog.json` to the overlay JSON file.",
          "type": "string"
        },
        "profile": {
          "$ref": "#/$defs/CatalogSpecSnapshotId",
          "description": "Profile snapshot this overlay describes."
        }
      }
    },
    "CatalogSnapshotValueOverride": {
      "description": "One value-space override scoped by the containing snapshot document.",
      "type": "object",
      "required": [
        "attribute",
        "values"
      ],
      "properties": {
        "attribute": {
          "description": "Attribute/property name whose value space is overridden.",
          "type": "string"
        },
        "values": {
          "$ref": "#/$defs/CatalogAttributeValues",
          "description": "Value space for this snapshot."
        }
      }
    },
    "CatalogSpecSnapshotId": {
      "description": "SVG specification snapshots understood by the catalog contract.",
      "oneOf": [
        {
          "description": "SVG 1.1 First Edition (W3C REC 2003-01-14).",
          "type": "string",
          "const": "Svg11Rec20030114"
        },
        {
          "description": "SVG 1.1 Second Edition (W3C REC 2011-08-16).",
          "type": "string",
          "const": "Svg11Rec20110816"
        },
        {
          "description": "SVG 2 Candidate Recommendation (2018-10-04).",
          "type": "string",
          "const": "Svg2Cr20181004"
        },
        {
          "description": "SVG 2 Editor's Draft (rolling).",
          "type": "string",
          "const": "Svg2EditorsDraft"
        }
      ]
    },
    "CatalogTreeSitterAttributeBuckets": {
      "description": "Attribute name groups that share a parser value shape.",
      "type": "object",
      "required": [
        "color",
        "coordinate_pair_list",
        "css_text",
        "functional_iri",
        "keyword",
        "length",
        "length_list",
        "length_list_or_none",
        "number",
        "number_list",
        "number_optional_number",
        "number_or_percentage",
        "path_data",
        "view_box"
      ],
      "properties": {
        "color": {
          "description": "Attribute values represented by the color parser.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "coordinate_pair_list": {
          "description": "Attribute values represented by coordinate pairs.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "css_text": {
          "description": "Remaining structured CSS grammars that should be parsed as CSS text.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "functional_iri": {
          "description": "Attribute values that can contain a functional IRI / URL reference.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "keyword": {
          "description": "Attribute values represented as bare keyword tokens.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "length": {
          "description": "Attribute values represented by a single length/percentage parser.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "length_list": {
          "description": "Attribute values represented by a length/percentage list parser.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "length_list_or_none": {
          "description": "Attribute values represented by `none` or a length/percentage list.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "number": {
          "description": "Attribute values represented by a bare number parser.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "number_list": {
          "description": "Attribute values represented by a list of bare numbers.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "number_optional_number": {
          "description": "Attribute values represented by one or two whitespace-separated numbers\n(CSS `<number-optional-number>`, e.g. `stdDeviation`, `baseFrequency`).",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "number_or_percentage": {
          "description": "Attribute values represented by number or percentage.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "path_data": {
          "description": "Attribute values represented by SVG path data.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "view_box": {
          "description": "Attribute values represented by an SVG viewBox.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "CatalogTreeSitterDocument": {
      "title": "CatalogTreeSitterDocument",
      "description": "Tree-sitter-facing projection of parser facts from the canonical catalog.",
      "type": "object",
      "required": [
        "attribute_buckets",
        "schema_version",
        "sources",
        "tokens"
      ],
      "properties": {
        "attribute_buckets": {
          "$ref": "#/$defs/CatalogTreeSitterAttributeBuckets",
          "description": "Attribute-name buckets consumed by `grammars/tree-sitter-svg`."
        },
        "schema_version": {
          "description": "Version of the JSON catalog/schema contract.",
          "type": "integer",
          "minimum": 0,
          "maximum": 65535,
          "format": "uint16"
        },
        "sources": {
          "$ref": "#/$defs/CatalogTreeSitterSources",
          "description": "Sources used to derive non-SVG grammar tokens."
        },
        "tokens": {
          "$ref": "#/$defs/CatalogTreeSitterTokens",
          "description": "Shared token sets consumed by `grammars/tree-sitter-svg`."
        }
      }
    },
    "CatalogTreeSitterSources": {
      "description": "Source provenance for generated grammar facts.",
      "type": "object",
      "required": [
        "css_unit_pages",
        "paths_html",
        "svg_clock_value_syntax",
        "webref_css"
      ],
      "properties": {
        "css_unit_pages": {
          "description": "CSS Values URLs used for unit extraction.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "paths_html": {
          "description": "Pinned SVGWG `paths.html` URL used for path-data grammar facts.",
          "type": "string"
        },
        "svg_clock_value_syntax": {
          "description": "SVG Animations URL used for SVG clock units.",
          "type": "string"
        },
        "webref_css": {
          "$ref": "#/$defs/CatalogPackageSource",
          "description": "CSS definitions source package."
        }
      }
    },
    "CatalogTreeSitterTokens": {
      "description": "Token sets shared by multiple generated grammar rules.",
      "type": "object",
      "required": [
        "angle_units",
        "color_interpolation_spaces",
        "color_spaces",
        "hue_interpolation_methods",
        "length_units",
        "path_command_letters",
        "time_units"
      ],
      "properties": {
        "angle_units": {
          "description": "CSS angle units from CSS Values.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "color_interpolation_spaces": {
          "description": "Color interpolation spaces accepted by CSS `color-mix()`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "color_spaces": {
          "description": "Predefined color spaces accepted by CSS `color()`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "hue_interpolation_methods": {
          "description": "Hue interpolation direction keywords.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "length_units": {
          "description": "CSS length units from CSS Values.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "path_command_letters": {
          "description": "Single-letter path command tokens from SVGWG `paths.html#PathDataBNF`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "time_units": {
          "description": "SVG clock value metric units from SVG Animations.",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "Discouraged": {
      "description": "Feature-scoped `WebDX` discouragement; independent of Baseline and BCD deprecation.",
      "type": "object",
      "required": [
        "according_to",
        "alternatives",
        "compat_key",
        "feature_id",
        "reason",
        "scope"
      ],
      "properties": {
        "according_to": {
          "description": "Supporting specification or vendor notices.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "alternatives": {
          "description": "Alternative Web Features identifiers.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "compat_key": {
          "description": "BCD compatibility key through which this feature applies.",
          "type": "string"
        },
        "feature_id": {
          "description": "Web Features identity, including when facts are aggregated across SVG contexts.",
          "type": "string"
        },
        "feature_name": {
          "description": "Human-readable feature name when provided upstream.",
          "type": [
            "null",
            "string"
          ]
        },
        "reason": {
          "description": "Upstream explanation, rendered as text.",
          "type": "string"
        },
        "reason_html": {
          "description": "Original HTML explanation, retained for consumers but not rendered as trusted HTML.",
          "type": [
            "null",
            "string"
          ]
        },
        "removal_date": {
          "description": "Optional upstream removal date; no inferred removal policy.",
          "type": [
            "null",
            "string"
          ]
        },
        "scope": {
          "$ref": "#/$defs/DiscouragementScope",
          "description": "Scope of the advice (always the whole Web Features feature)."
        }
      }
    },
    "DiscouragementScope": {
      "description": "Scope of `WebDX` discouragement metadata.",
      "oneOf": [
        {
          "description": "Advice applies to the named Web Features feature.",
          "type": "string",
          "const": "feature"
        }
      ]
    },
    "FlagKind": {
      "description": "BCD's two flag categories.",
      "oneOf": [
        {
          "description": "A browser preference.",
          "type": "string",
          "const": "preference"
        },
        {
          "description": "A command-line/runtime flag.",
          "type": "string",
          "const": "runtime_flag"
        }
      ]
    },
    "Unsupported": {
      "type": "boolean",
      "const": false
    },
    "VersionAdded": {
      "description": "BCD's `string | false` version value, including qualified versions and `preview`.",
      "anyOf": [
        {
          "description": "Original version string.",
          "type": "string"
        },
        {
          "$ref": "#/$defs/Unsupported",
          "description": "Explicitly unsupported."
        }
      ]
    }
  },
  "title": "svg-data catalog v2",
  "description": "Root manifest written to `svg-data/data/catalog.json`.",
  "type": "object",
  "required": [
    "commit",
    "core",
    "graph",
    "schema_version",
    "tree_sitter"
  ],
  "properties": {
    "commit": {
      "description": "The upstream commit this catalog was derived from.",
      "type": "string"
    },
    "compat": {
      "description": "Browser-compat provenance and retained compat subfeatures.",
      "anyOf": [
        {
          "$ref": "#/$defs/CatalogFileRef"
        },
        {
          "type": "null"
        }
      ]
    },
    "core": {
      "$ref": "#/$defs/CatalogFileRef",
      "description": "Canonical latest element/attribute catalog document."
    },
    "graph": {
      "$ref": "#/$defs/CatalogFileRef",
      "description": "Derived relationship graph document."
    },
    "schema_version": {
      "const": 2
    },
    "snapshots": {
      "description": "Version-specific overlay documents, sorted by profile.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/CatalogSnapshotRef"
      }
    },
    "tree_sitter": {
      "$ref": "#/$defs/CatalogFileRef",
      "description": "Tree-sitter parser projection document."
    }
  }
}
